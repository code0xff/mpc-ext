# extension

크롬 확장 (Manifest V3). Phase 2에서 구현한다 (`docs/roadmap.md`).

스택은 TypeScript + React + Vite + WXT (`docs/adr/0002-stack.md`).
셰어는 background service worker 메모리에만 두고, 저장은 항상 암호화한다 (`docs/security.md`).
