# Secret scanning. Real keys and secrets never get committed (AGENTS.md).
[extend]
useDefault = true

[[allowlist]]
description = "Dummy values and examples in documentation"
paths = ['''docs/.*\.md''', '''README\.md''', '''web/.*''']
